#include <stdio.h>

int main(){ 

    int digit_sum = 0;
    int digit_count = 0;

    // read in characters


    printf("Input contained %d digits which summed to %d\n",
            digit_count, digit_sum);


    return 0;

}
